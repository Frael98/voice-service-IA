info:
  name: text-to-voice-streaming
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8000/text-to-voice-streaming
  body:
    type: json
    data: |-
      {
      "text": "Este es un texto de prueba"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
