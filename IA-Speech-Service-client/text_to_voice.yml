info:
  name: text_to_voice
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8000/text-to-voice
  body:
    type: json
    data: |-
      {
        "text": "Hola ."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
