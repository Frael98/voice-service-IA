info:
  name: Login
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8000/v1/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
