info:
  name: speech_to_text
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/speech-to-text
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
